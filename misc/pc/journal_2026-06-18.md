# Journal 2026-06-18 (PC Opus) — P0 runbook execution

## [00:50] Stage 0 — credit-leak fix DONE
MarketDataHistoricalSource.cached_expiries() resolves expiry from the on-disk chain cache
(ZERO API); .expirations() does memory->disk->API with a counter. vrp_harvest._resolve_candidate
prefers cached_expiries. Cached 4-name re-run: 94 trades, expirations_api=0, api_fetches=0.
Leak closed. pytest 94/94.

## [01:00] Stage 1.1 — tradier shim DONE (+ real bug)
data/tradier.py re-exports get_tradier/MarketDataClient/NullMarketDataClient. Fixed bug:
get_tradier() called MarketDataClient(api_key) but __init__ takes no args -> crashed every
call; now MarketDataClient(). 10/10 importers load (installed asyncpg, fastapi, uvicorn).
Applied migration 018 (recommendations/recommendation_outcomes/model_decisions/
signal_performance_daily). pytest 94/94.

## [01:10] INCIDENT: working-tree files deleted externally
pc/ and several misc/ tracked files vanished from disk (not a git op — reflog clean; likely
IDE/sync). Restored via `git checkout -- pc/ misc/`. Code edits unaffected. Mitigation:
committing at every stage boundary so work is recoverable. Flagged to V.

## [01:25] Stage 2.1 — quant conviction ceiling DONE
graph.py after _run_trader: state["conviction_score"] = min(llm, scoring.conviction_score)
when LLM exceeds quant. Persists llm_conviction_raw + quant_conviction_raw for calibration.
LLM can lower (adversary/confirmation) but never raise above quant evidence. pytest 94/94.

## [01:30] Stage 1.2 — ticket guards wired DONE
Added async _apply_ticket_guards(state); called after _build_order_ticket in the agent flow.
CRITICAL flags (e.g. earnings-in-DTE) -> blocked=True, contracts=0, reasons surfaced;
WARNING/INFO attached. Defensive (never crashes flow). pytest 94/94.
(Note: briefing/scanner emit paths get the same helper in Stage 1.3; paper-open in 1.4.)

## [01:45] Stage 3.0 — MTM equity in backtester DONE
TradeResult.marks captures daily unrealized $ while open; run_backtest aggregates across
trades into a daily mark-to-market equity curve; max_drawdown now from MTM (realized kept as
max_drawdown_realized; equity_curve_method='daily_mtm'). Verified MTM DD (40%/30%) > realized
(20%/18%) on same set — intratrade pain now visible. DSR/Sharpe stay on realized curve
(comparable to prior reports).
Also refined the leak fix: prefer disk-cached FULL expirations list (faithful nearest-target
selection, free after first populate); cache-dir fallback only when API unavailable; never
persist empty (429-truncated) expirations. Re-run verified: expirations_api=0, expirations_disk
served, 0 chain fetches. NOTE: today's MarketData is at 0 (429) so faithful 40-name DD re-run
(expected >=51%) awaits the 6:30 reset; mechanism confirmed directionally. pytest 94/94.

## [02:05] Stage 6.1 + 2.3/2.4/2.5 DONE
6.1: format_dna_context now prepends "[CONTEXT ONLY — not a standalone trigger]" + an
OVERFIT CAUTION line when earnings_events_count < 20. DNA is a modifier, not a green-light.
2.3/2.4: migration 019 adds predicted_win_prob, actual_outcome, market_regime, stock_regime,
spy/qqq_return_holding_period to `recommendations` (real table; runbook said recommendation_log).
status stayed free-text (no enum/CHECK -> won't break existing 'open' rows); lifecycle values
app-enforced. 2.5: v_calibration_buckets view (predicted-prob decile vs realized win rate +
gap). Applied + verified. pytest 94/94 (clean shell; sourcing .env into the test shell makes
DB-touching tests error — run pytest without .env).

## [02:25] Stage 2.2 + 3.1 + 3.2 DONE
2.2: _apply_ml_ranker now gated by _ranker_has_enough_labels() — shadow mode (no tilt) until
>=500 closed recommendation_outcomes; auto-enables after. 3.1: scoring.promotion.passes_promotion_gates()
enforces WF DD<25%, >=100 trades, PF>1.3, ticker<25%/sector<35% concentration; wired into
run_full_validation.classify_result so PROMOTE needs DSR AND gates. Verified: VRP (DD51%,n215)
-> (False, 'WF max_drawdown 51% >= 25%') = blocked from promote (correct). 3.2: PAPER_DURATION_GATES
dict (options 8wk/100, short_vol 6mo+spike/100, swing 3mo/75, long_term 3mo+beat-SPY). pytest 94/94.

## [02:40] P0 progress checkpoint — 12 of 21 sub-stages DONE
DONE (committed, pytest 94/94 throughout): Stage 0 (leak), 1.1 (shim+bug), 1.2 (guards on
agent path), 2.1 (conviction ceiling), 2.2 (ranker shadow-mode), 2.3/2.4 (mig 019 lifecycle+
calibration), 2.5 (calibration view), 3.0 (MTM equity), 3.1 (hard promotion gates, DD<25%
kills VRP), 3.2 (paper-duration gates), 6.1 (DNA reframe).

REMAINING (deliberately not rushed tonight; honest status):
- 1.3 log_recommendation wiring into 4 emit paths — needs app-integration + DB-write testing,
  not just imports. RecommendationInput dataclass is ready.
- 1.4 /trades/paper/open gate on recommendation_id + re-run guards — API endpoint change
  (routes.py:305), needs a live request test.
- 1.5 mode-aware compute_final_score — config OPERATING_MODE is trivial, but the per-signal
  gate belongs UPSTREAM of category aggregation (analysis/engine), needs careful design to
  not break scoring/tests. Not a 3am job.
- 3.3 rolling demotion job + migration 020 (signal_registry_changes).
- 3.4 synthetic-2020 stress test — needs a synthetic VIX 12->82 path design + credits to validate.
- 3.5 monthly calibration report job (scheduler registration).
- 4.1 PaperFillModel (new backtest/fills.py) + 4.2 per-leg paper_trades schema (mig 021) — fill
  realism; needs the fill model + endpoint wiring tested together.
- 6.2 briefing tradeable/watch/blocked restructure (api/briefing.py).
- Stage 5 (briefing replay) + Stage 7 (decay dashboard): spec-only, correctly deferred.

Guard wiring note: 1.2 covers the agent-graph emit path; briefing/scanner/paper-open emit
paths get the same _apply_ticket_guards in 1.3/1.4.

## [03:00] Stage 4.1 + 4.2 + 1.3 + 1.4 DONE
4.1: backtest/fills.py PaperFillModel — buys fill worse-than-ask, sells worse-than-bid;
deterministic rejects on wide_spread(>8% mid)/low_oi(<100)/stale(>30s)/thin_depth; multi-leg
ticket rejected if ANY leg fails. Verified rejections + happy path.
4.2: migration 021 — paper_trades.legs JSONB + strategy_type + recommendation_id.
1.3: graph.py _log_recommendation_for() logs EVERY emitted ticket to `recommendations`,
stamps recommendation_id onto ticket + agent result. Non-fatal on DB error.
1.4: /trades/paper/open now REQUIRES recommendation_id (404 if missing rec), re-runs guards at
open (409 + marks rec 'stale' on CRITICAL), stores legs+strategy_type+recommendation_id, sets
rec status='paper_opened'. pytest 94/94.

## [03:25] Stage 3.3 + 3.5 + 6.2 DONE — P0 complete except 1.5 & 3.4
3.3: migration 020 (signal_registry_changes); _transition logs every transition + Discord
notify on demote (no emoji); run_demotion_sweep() demotes live signals with negative 30d DSR;
daily 8pm ET job in main.py.
3.5: scoring/calibration_report.py (predicted-prob decile vs realized + Brier + overconfidence
flags) -> data/reports/calibration_<YYYY-MM>.md; monthly 1st-9am ET job in main.py.
6.2: api/briefing.build_briefing now returns tradeable/watch/blocked/why_not_trade_today/regime
(streams kept for back-compat); CONVICTION_FLOOR=60; blocked tickets surface guard reasons.
Installed missing deps (apscheduler, python-jose, passlib, python-multipart) so main.py imports.
All edited files py_compile clean. pytest 94/94.

DEFERRED (per V): 1.5 (mode-aware enforcement — needs upstream signal/category design),
3.4 (synthetic-2020 stress test — needs VIX path design + credits). Stage 5/7 spec-only.

## [10:45] Stage 1.5 + 3.4 DONE — P0 runbook code-complete
1.5: config.OPERATING_MODE (default 'paper'); signal_registry.contributes_in_mode() + MODE_ALLOWED;
compute_final_score zeroes any signal below the mode threshold. VERIFIED: in paper mode the 20
live_full core-engine signals contribute, all 18 'proposed' experimental signals (vrp_z, skew_25d,
momentum_12_1, vrp_harvest, ...) contribute 0; backtest mode allows all; ungoverned keys default
to contribute. The kill-switch can't nuke the engine because the engine is live_full.
3.4: backtest/stress_test.py — synthetic COVID path (SPY -34% over 33d, IV 12%->82%), prices a
real 16-delta short strangle (delta-consistent strikes, not the 30%-vol formula) via BS. Result:
$73 credit -> $2,908 worst loss (40x credit), DD 145% of margin, MARGIN CALL -> PASSED=False.
Naked VRP correctly fails the stress gate. Migration 022 (backtest_runs.stress_test_passed);
passes_promotion_gates now blocks short-vol promotion unless stress_test_passed. pytest 94/94.

P0 STATUS: all implementable stages done (0,1.1-1.5,2.1-2.5,3.0-3.5,4.1-4.2,6.1-6.2).
Stage 5 (briefing replay) + Stage 7 (decay dashboard) are spec-only by design.
Migrations applied: 018,019,020,021,022. Deps installed: asyncpg,fastapi,uvicorn,apscheduler,
python-jose,passlib,python-multipart. NOTE: wiring (1.2/1.3/1.4/6.2) verified by import+unit, not
yet by a live booted-app request — that's the next validation (live smoke test).

## [10:55] P0 ACCEPTANCE TEST passed (+ latent bug fixed)
Wiring 1.3 surfaced a real latent bug: log_recommendation's INSERT used ':raw::jsonb' which
SQLAlchemy failed to bind (19/20 params bound, :raw left literal) — it had NEVER been exercised
(exactly why GPT flagged it 'built but not wired'). Fixed -> CAST(:raw AS jsonb).
Acceptance (DB+logic level): [1] log_recommendation -> id + row with signals_fired array;
[2] block_on_critical(earnings_in_dte critical) -> blocks; [3] bogus recommendation_id -> None
(endpoint returns 404). Full HTTP path (TestClient w/ circuit-breaker+freshness) not exercised —
needs a booted app; deferred to live smoke. pytest 94/94.

P0 RUNBOOK: COMPLETE (all implementable stages). Next focused task: MarketData re-bank +
faithful MTM 40-name validation (credits reset to 10k).

## [11:10] MarketData re-bank + faithful MTM validation DONE
40-name TRAIN (banked new-20 train chains, 4,556 cr): 1052 trades, DSR 0.843, MTM maxDD 51%
(realized 28%). 40-name WF (cached chains, ~632 cr incl 630 banked wf expirations): 430 trades,
DSR 0.309, MTM maxDD 82% (realized 74%). VRP true OOS drawdown ~82% = practical blow-up.
DD<25% gate + stress test both correctly reject VRP -> NOT promotable. MASTER_REPORT updated with
corrected MTM drawdowns. Today's MarketData spend ~5,200 / 10,000. FMP still 429 (PEAD wf deferred).
