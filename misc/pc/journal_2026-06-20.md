# PC Opus journal — 2026-06-20 (0619.3 RUNBOOK: adapters + full sweep + daemon fix)

Owner: PC Opus (full dev authority). Decision-maker: V. Priority depth-on-leads then breadth (V).
Standing rules: no promotions, SANDBOX-cap all (no PIT yet), data-gated => labeled not NO_EDGE,
universe>6000 guard, real ADV ranking (no alphabetical), pytest 94/94, no emojis, no new registry signals.

---

## Pre-flight pushbacks (all surfaced to V, accepted)
1. macro/calendar/regime_markov_market/sector_dispersion are MARKET-level, not XS long-short ->
   wrong harness; treat as timing/overlay. Honest registry-XS count ~30-35 not 42.
2. DATA_UNAVAILABLE sentinel ONLY on true no-data (empty/404), never 429/5xx (else rate-limit burst
   corrupts the bank with fake gaps).
3. ADV ranking built FREE from banked FMP profiles (averageVolume*price) — no credits spent ranking.
4. Sequencing: 397 credits left -> adapters+sweeps first (free/cache), daemon fix banks at reset.
5. Scope honesty: 25+ components; build highest-value-first, label the rest.

## BUILT (committed, pytest 94/94)
- **liquid_universe.py** — ADV rank from 6,740 banked FMP profiles; top-N slices + sector map.
- **options_xs.py** — vrp_z, vrp_level, iv_call_put_spread, iv_term_slope (BS-inverted IV from cached
  chains, documented academic framings).
- **equity_xs.py** — 11 primitives (RSI/MACD/Stoch/ROC/EMA/slope/ADX/BB/ATR/RVol/support) + trend/risk
  rollups, cross-sectional, PIT.
- **fmp_cache.py** + wired pead & insider disk-first (read the daemon's bank, no per-fold re-fetch).
- **chain daemon fix (Track C)** — full ADV work-list (6,768, never runs dry) + DATA_UNAVAILABLE
  sentinel (probe-first, true no-data only). Restarted; resumes at reset.
- **FMP daemon (Track D step1)** — added balance_sheet + cash_flow endpoints, bumped statement
  priority (33-37) so fundamentals bank overnight. Restarted.
- **full_sweep.py** — one comparable harness (MTM/costs/train-WF/SANDBOX-cap), depth+breadth+fmp,
  incremental + resumable. Output FULL_SWEEP_2026-06-20.md + full_sweep_progress.json (trackers ingest).

## SWEEP RESULT — 33 variants, identical methodology, ZERO clear the gates
Registry signals tested 5 -> 12/49 (+11 research primitives + multi-slice momentum). Dominant
signature: regime-dependence (strong one window, dead the other). Strongest incumbents (GPT-18 bar):
- skew_25d wf 0.67 @ 10% DD (train-dead) | iv_call_put_spread wf 0.65 @ 1% DD (train-dead)
- momentum_12_1(126,top250) wf 0.60 (train-dead, NOT stable across slices)
- pead hold=10 train 0.54 / wf 0.38 but 79% DD | insider wf 0.85 but n=6/12 (too small)
None clears train>=0.5 AND wf>=0.3 AND DD<25% AND n>=100. GPT-18 "worth it" bar = beat ~0.65 wf at
<25% DD with non-dead train and adequate n — a STABLE edge, which no incumbent is.

## HONEST CORRECTIONS
- **Momentum reversal:** last night's "lookback=126 clears all gates on liquid_264" does NOT replicate
  on ADV slices or across the neighborhood {105,126,147,168}x{100,250,500}. Train DSR dead everywhere.
  Universe-composition artifact. This is exactly why the runbook mandates the whole neighborhood.
- **Primitives:** no single one carries robust edge (best slope/roc wf ~0.22-0.29, train-dead).
- **vrp_z/vrp_level:** train 0.90 / wf 0.01 / 63% DD = overfit, not edge.

## LABELS (data-gated, NOT no-edge)
- CACHE_LIMITED: options XS on 169-name chain cache (n_wf~86). iv_term_slope CHAINS_LIMITED (n_tr=0).
- FUNDAMENTALS_PENDING: fundamental/quality not run — statements banking overnight (priority bumped).
- short_squeeze PENDING: free exchange short-interest CSV ingester (Track E) NOT built tonight — the
  one genuine remaining data gap (FMP SI = 404). SME call: a clean PENDING beats a fragile half-built
  FINRA-CSV parser at hour N; documented for a dedicated build. (insider_cluster IS tested via FMP.)

## DAEMONS RUNNING OVERNIGHT
- Chain daemon: floored at reset last night, restarted with full ADV work-list + sentinel; resumes at
  MarketData reset (~13:30 UTC) and banks ~100 names/day on the 6,768-name list.
- FMP daemon: restarted; banking income/balance/cashflow (fundamentals) + remaining endpoints.
- Equity cache: 537 names (top-100/250 complete, top-500 ~95%).

pytest 94/94 throughout. signal_registry untouched. Nothing promoted.
