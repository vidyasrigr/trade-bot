# PC Opus journal — 2026-06-19 (CONSTRAINT_RUNBOOK execution)

Owner: PC Opus. Decision-maker: V. Mode: 4 parallel tracks per CONSTRAINT_RUNBOOK_2026-06-19.
Standing rules: no promotions (V's call), no live capital, pytest 94/94, no emojis, persistent caches on.

---

## Pre-flight: runbook alignment + FMP tier verification

- Read CONSTRAINT_RUNBOOK + TRACKERS_SPEC + fmp_daemon.py. Runbook now folds in all my Day-0
  pushback (FMP tier reality, universe-resolver gating dependency, yfinance throttling, primitive
  scoping, PIT-as-SANDBOX fallback). Aligned.
- **FMP Starter probe (live, key from backend/.env):** key works on new tier. Findings vs runbook:
  - 200 + real rows on NVDA: earnings (epsActual/epsEstimated), profile, **insider-trading/search
    (5 Form-4 rows)**, **grades-historical (5 rating rows)**, income, news, float, price_target.
  - 402 (premium *param*, fixed -> annual): analyst-estimates, ratios, key-metrics with period=quarter.
  - 402 (tier-locked, skip): earnings-surprises-bulk -> compute surprise per-symbol from `earnings`.
  - 404 (path/coverage, not tier): short-interest -> free exchange CSV (Track 4a).
  - Net: the $29 unblocks MORE than predicted — insider_cluster + analyst_revision_cascade have a
    real FMP source (verified non-empty per V's empty-200 caution). EDGAR stays a free cross-check.

## TRACK 2 — FMP daemon (DONE: fixed + launched)

- Fixed premium-gated params: ratios/key_metrics/analyst_est -> annual (period=quarter is 402).
- Hardened probe-fail path: a failed endpoint is marked dead and skipped for the rest of the run
  (was retrying every tier-locked symbol every pass = thousands of wasted calls).
- Bug found + fixed: `data/universe.py` dropped the ENTIRE nasdaqlisted.txt (footer NaN symbol blew
  up the regex via astype(str)); universe was silently NYSE-only (2637). dropna() fix -> 6908 names
  incl. AAPL/MSFT/NVDA. Added Redis-free `fetch_listed_symbols()` so the daemon needs no Redis.
- Launched full-universe daemon in background (pid recorded in _daemon_stdout.log).
  Probes: earnings/profile/grades/income OK; earnings_surprise 402 + short_interest 404 auto-disabled.
  Throughput ~280/min. Earnings banked >880 within minutes.

## TRACK 5 — Trackers (DONE: built + scheduled)

- Added `streams` (O/S/M/L) to SignalSpec, data-driven from one `_SIGNAL_STREAMS` map applied via
  replace() (all 49 stamped: O19 S23 M18 L12; `streams_for()` helper).
- Built `scripts/build_trackers.py` -> SIGNAL_STATUS.md, DATA_INVENTORY.md, VALIDATION_LEDGER.md.
  Pure-python, read-only, <5s, degrades to PENDING/0% on missing source. Reads REGISTRY +
  master_progress.json (machine-readable validation verdicts) + cache dirs (marketdata_cache, fmp,
  feature_store both roots). Writes to data/trackers/ and misc/mac/trackers/.
  - Verdict logic enforces the hard gates (train>=0.50, wf>=0.30, MTM-DD<25%); free/equity sweeps
    are survivorship-capped at SANDBOX (never PASS) per the PIT gap.
  - DATA_INVENTORY labels FMP ratios/key_metrics/analyst_est as annual-fundamentals (V's caution #2).
- Wired hourly regen into main.py APScheduler (id=rebuild_trackers, :05).
- First gen matches MASTER_REPORT_MTM: skew_25d 0.00/0.75/5% SANDBOX, momentum_12_1 0.00/0.60/18%
  SANDBOX. Tested 4/49, nothing PASSes (correct).

pytest 94/94 after Tracks 2+5.

## TRACK 4b — skew_25d regime-conditioning (DONE: honest REJECT)

- Pre-registered H1 (edge concentrated in calm/risk-on regimes) in SKEW_REGIME_PREREG and committed
  BEFORE running anything (849c76c). Built scripts/skew_regime_experiment.py — runs skew on the 49/51
  cache (0 credits), tags each trade by regime_classifier.regime_tag(entry_date) + window, computes
  per-bucket DSR via run_equity_backtest. num_trials=#buckets for deflation; MIN_N=30.
- **Result: H1 REJECTED.** Hypothesized calm-bull bucket (normal_vol|bull) is DEAD in train
  (DSR 0.027/0.006). Train edge is in BEAR buckets (normal_vol|bear 0.245/0.989); WF edge is in
  normal_vol|range (0.664/0.468). Train and WF winning buckets DISAGREE -> no stable regime story.
  Pooled WF rests on 7-8 cohorts; the apparent strength is a small-sample artifact.
- Per the pre-registered decision rule (different bucket won -> new hypothesis, not promotion):
  skew_25d stays SANDBOX. Real blocker is statistical power (n per bucket), not regime. Re-test when
  Track 1 grows the bank to 200+ names. Appended to MASTER_REPORT_MTM.
- This is the value of pre-registration: looking first would have tempted crowning normal_vol|range
  (wf 0.66) or normal_vol|bear (train 0.99) as "the regime" — but they're different buckets = mining.

pytest 94/94.

## TRACK 3 — free-signal sweep on liquid_264 (DONE: momentum; lead_lag finishing)

- Gating dependency resolved two ways:
  - Built backtest/equity_cache.py (persistent per-symbol OHLCV parquet) + rewired _close_panel to
    read cache-first with yfinance write-through. Backtests now repeatable + throttle-free.
  - yfinance was throttling (cumulative usage) even single-name -> added MarketData get_history
    fallback (~1 credit/name). Backfilled the curated liquid_264 (chain-bank CORE_200 +
    get_full_universe) 264/264, 0 failures, via MarketData. ~264 credits (2.6% of budget).
- HONEST SCALE CALL: did NOT fake liquid_500/1000/2000 from the alphabetical directory head (that's
  illiquid junk -> garbage IC, worse than honest). Ran the sweep on liquid_264 (2.5x the old 107 cap).
  True ADV-ranked breadth needs volume ranking = a later expansion. Documented in the report.
- Survivorship: currently-listed names only -> every result capped at SANDBOX (never PASS), DSRs are
  upper bounds. Per the PIT gap.
- Built scripts/free_sweep.py (incremental per-variant persist so a slow variant can't sink the run;
  first attempt lost everything to a 5-min timeout on lead_lag's O(n^2)). Output FREE_SWEEP_2026-06-19.md
  + free_sweep_progress.json (trackers ingest it).
- **RESULT (momentum_12_1 on liquid_264, MTM):**
  - lookback=126: train 0.678 / wf 0.332 / wf-DD 15% -> CLEARS ALL HARD GATES (would PASS but for
    survivorship -> SANDBOX-capped). The 107-name cap was MASKING this edge. Most interesting free find.
  - lookback=252: 0.277/0.751/8% (strong wf, weak train -> partial)
  - lookback=189: 0.083/0.746/5% (same pattern)
  - lookback=63: 0.485/0.202/35% -> NO_EDGE
- lead_lag (top120) still running in background (O(n^2), ~30min); writes its row + updates the report
  on completion. Only the pure-free generators run here; pead/insider/squeeze run as FMP readers land,
  skew is options-bound; trend/candles/etc. have no backtest adapter yet (the ~15-adapter build).

## TRACK 4a — free auxiliary ingest (PARTIAL: FRED done; CFTC/EDGAR deferred)

- FRED expanded 11 -> 33 series (rates/curve, inflation/breakevens, vol/financial-conditions/stress,
  credit, growth/labor, money/liquidity, fx/commodities). All 33 ingested full-history, 0 failures.
  Tracker DATA_INVENTORY shows FRED 33/30 = 100%.
- CFTC COT: module data/cftc_cot.py already exists (Socrata, Redis-cached, works in-app). cot_extreme
  is a feature_only signal -> cannot be promoted, and the validation runner doesn't cover feature_only.
  Building a feature-store backtest adapter for it is low-value tonight; DEFERRED. Noted for V.
- EDGAR Form 4 / exchange short-interest CSV second-sources: DEFERRED. insider_cluster already has a
  verified FMP source (real rows); the genuine gap is short-interest (FMP 404) for short_squeeze —
  exchange SI-CSV ingest is real work, queued for a later night. Prioritized Track 3 (validatable
  signals) over second-sources for signals that already have a source.

## TRACK 1 — chain-banking daemon (DONE: built + launched)

- IMPORTANT: MarketData credits had already RESET (remaining=10000 at 19:45 local) — so banking
  runs TONIGHT, not waiting for morning.
- Added rate-limit header capture to MarketDataClient (_capture_rate_limit reads
  X-Api-Ratelimit-Remaining/-Reset on every _get) so the daemon knows credits + reset time.
- Built scripts/chain_bank_daemon.py. First design banked via run_vrp_backtest (VRP gate) but that
  (a) needs yfinance prices [which were throttling -> 0 candidates] and (b) only banks chains where
  VRP *enters* -> under-banks the cross-sectional signals (skew/vrp_z/iv_term) that are the priority.
  REWROTE to bank monthly ~45-DTE chain SNAPSHOTS via the cached SPY calendar (0 credits) +
  expirations (counted) + _load_chain (persists parquet). No yfinance dependency.
- Verified on QCOM: 42/60 snapshots, 80 credits (1/expiration + 1/chain), 38 parquets >3k. So
  ~80-120 cr/name -> ~85-120 names/day (better than the 38/day estimate; old estimate assumed VRP
  daily-mark depth, we bank breadth-first snapshots).
- Extended CORE_200 seed to 222 deduped liquid optionable names (50 already banked, 172 targets).
  Daemon spends to FLOOR=150 then sleeps until reset (header ts, fallback 13:35 UTC) + auto-resumes.
- Launched in background (pid logged in _bank_stdout.log). 172 names queued; ~85-120 bank tonight,
  remainder auto-fills over next 1-2 days. Idempotent + resumable.

pytest 94/94.
