[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --no-header --tb=short --strict-markers
asyncio_mode = auto
markers =
    slow: long-running tests (>1s)
    integration: requires DB/Redis/external API (skipped in CI)
