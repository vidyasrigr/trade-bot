# Journal 2026-06-17 (PC Opus, owner of signal validation)

Role: I own all validation decisions except live-capital, gate-threshold,
strategy-family, and budget calls (V's). Format: Decision / Why / Action / Result / Next.

---

## [18:01 PT] Decision: Take ownership; read context, audit the laptop foundation
Why: V handed full ownership of signal validation. Must inherit context before acting.
Action: Read SESSION_CONTEXT.md, PC_OPUS_HANDOFF.md, pc/log.md (yesterday), misc/PENDING.md,
        and backend/scripts/run_full_validation.py (laptop's orchestrator skeleton).
Result: Foundation is sound but has 3 real bugs I now own:
        (1) generic equity dispatch passes universe both positionally AND via **config
            (config carries a "universe" key) -> TypeError at runtime.
        (2) REPORTS_DIR is cwd-relative -> would split reports from yesterday's when run
            from backend/. Cache lives at backend/data/marketdata_cache; reports at
            root/data/backtest_reports. Two data dirs — must reconcile.
        (3) Expects modules that don't exist: equity_engine, momentum_xs_v2, pead, insider,
            squeeze, lead_lag_bt, pre_fomc_straddle, and an IC `wings_sigma` param my
            vrp_harvest_ic.py doesn't expose (I built wing_sigma_mult + fixed WING_VOL).
        Also confirmed from yesterday: cached chains have mid_iv==0 AND delta==0 (no
        historical greeks on Starter) -> skew/term/IV-spread family blocked until I write
        a BS-inversion module (V pre-authorized this).
Next: Build Phase A harnesses, pytest after each. Start with equity_engine.py.

## [18:01 PT] Decision: Consolidate log to pc/log.md
Why: V's docs reference pc/log.md; the actual file was at repo-root log.md.
Action: mv log.md pc/log.md. Going forward append technical events there; decisions here.
Result: Single log location matching V's convention.
Next: equity_engine.py.

## [18:05 PT] Decision: Build equity engine + cross-sectional harness, run VRP sweep on cached-20 (free) first
Why: No equity-signal backtester existed; and concurrent VRP variants each building
     their own MarketData source would re-fetch the same uncached chains N times
     (could be 30k+ credits). Brain-on credit discipline says: find the best wing/stop
     on the FREE cached-20 first, spend credits in Phase C only on the winner.
Action: Built backtest/equity_engine.py (cohort-level Sharpe/DSR, position-level
        win/pnl), momentum_xs_v2.py, pead.py, insider.py. Made vrp_harvest_ic.py
        parametric on wings_sigma (distance from spot in sigma units). Refactored
        scripts/run_full_validation.py: shared VRP source + sequential VRP execution,
        REPORTS_DIR pinned to repo-root, fixed the universe-kwarg collision bug,
        replaced alphabetical get_scan_universe()[:N] with curated get_full_universe
        (~150 liquid names — the laptop draft would have fed momentum illiquid junk),
        dropped pre_fomc (options structure, wrong contract), added --vrp-universe.
Result: pytest 94/94 green throughout. Equity engine + momentum + PEAD validated
        end-to-end. Launched master_v1 (14 variants, cached20, budget 6000, conc 4).
Next: While it runs, build BS-inversion to unblock skew family; build lead_lag_bt.

## [18:10 PT] Decision (FINDING): FMP legacy API is DEAD app-wide; insider/squeeze BLOCKED on V's tier
Why: PEAD + insider generators returned 0 trades. Per the rules, 0 trades = bug, not verdict.
Action: Probed FMP endpoints directly.
Result: ALL legacy v3/v4 endpoints return HTTP 403 "Legacy Endpoint - no longer
        supported... only for subscriptions prior Aug 31 2025." This breaks EVERY live
        FMP module in the app (insider_flow, short_squeeze, fundamental, lt_scoring,
        stock_dna, analyst_targets, calendar) — they all call legacy paths. The new
        "stable" API works with V's key:
          - stable/earnings?symbol=X -> 200, 163 quarters actual vs estimate EPS. PEAD repointed; WORKS.
          - stable/insider-trading/search -> 402 RESTRICTED (not on V's FMP tier).
          - stable/profile -> 200 (key valid).
        => PEAD unblocked. insider = BLOCKED (data not on tier). squeeze = BLOCKED
        (needs historical short interest, also legacy/restricted + no PIT time series).
Next: ESCALATION-WORTHY for V (budget/subscription decision): (a) FMP insider/short-interest
      need a tier upgrade to validate those signals; (b) the live app's FMP modules are
      silently broken and need migrating to the stable API (separate work item, logged).
      I will NOT spend on a tier upgrade — that's V's budget call. Noted in MASTER_REPORT.

## [18:20 PT] Decision: Built BS-inversion module; skew/IV family unblocked
Why: V pre-authorized a BS-inversion to recover IV from cached mid prices since
     Starter has no historical greeks. High value, free (uses cached chains).
Action: Wrote backtest/iv_inversion.py (bs_price/bs_delta/implied_vol via Brent +
        enrich_chain_iv). Validated on a cached SPY chain (2022-10-12).
Result: ATM IV ~30% (correct for Oct-2022), 25d put IV 33.3% vs 25d call 27.2%,
        skew_25d = +6.1% (correct equity negative-skew smirk). 262/324 strikes
        invert cleanly. skew_25d / iv_call_put_spread / iv_term_slope now buildable
        from cached chains for $0.
Next: Read master_v1 results when done; if PEAD/IC promising, expand in Phase C.
      Build skew_25d cross-section backtest (uses this module) in Phase C.

## [18:30 PT] Decision: master_v1 read — IC dead at all wings, stops don't help, PEAD promising
Why: First full sweep landed. Act on it (Phase C) without waiting for V.
Result (cached-20, $0):
  - VRP naked baseline reproduced EXACTLY (train DSR 0.904 / wf 0.317) -> harness validated.
  - IC wings 1.5/2.0/2.5/3.0 sigma: ALL DSR=0, negative PnL. Defined-risk-via-wings is
    dead for VRP at every distance. Caps DD (~12-16% wf) but always loses money.
  - Stops 1.0x/1.5x: IDENTICAL trade count (526/215) and DD slightly WORSE (51->53->56%).
    The EOD stop never binds; 21-DTE/profit exits dominate. And % DD is scale-invariant
    so sizing can't fix it. => neither obvious structural fix works.
  - PEAD hold=10d train DSR 0.527 (>gate!) but wf=0 (rate-limit bug). momentum wf-strong/
    train-dead (regime-dependent).
Action: Fixed PEAD FMP cache (process-level, stops train+wf double-fetch rate-limit).
        Reclassified missing-module/402 as BLOCKED not ERROR. Built lead_lag_bt, squeeze
        stub. Added skew_25d + momentum 6-1/3-1 to the grid.
Next: regime-gate experiment (the one untested lever); clean re-run.

## [18:40 PT] Decision (post-hoc): regime-gated VRP entry — the only fix that keeps the edge
Why: IC and stops failed; the remaining lever is entry timing. POST-HOC (added after
     seeing the DD problem) — flagged per rule 3.
Action: Built backtest/strategies/vrp_regime.py (skip entries when SPY 5d realized vol >
        ratio x 20d realized vol; PIT, market-level). Tested standalone on cached-20.
Result ($0 credits): ratio=1.5 -> train DSR 0.831 / wf 0.321, wf DD 48.2% (vs naked 51%).
        ratio=1.3 -> train 0.690 / wf 0.320, wf DD 47.0%. Keeps the edge AND stays above
        the gate, but only trims the tail modestly (~3-4 pts). The correlated EOD tail is
        only partly timeable with a simple vol-accel filter.
Verdict: regime gate is the best VRP tail mitigation found, but not a full fix. VRP stays
         paper-eligible as a SMALL sleeve with the regime gate; deeper fix (VIX term-
         structure backwardation gate, or cap on concurrent positions) is the next experiment.

## [18:50 PT] Decision (FINDING + fix): master_v2 panel-fetch flakiness -> false 0-trades
Why: v2 returned PEAD/lead_lag = 0 trades though they work standalone. 0 trades = bug.
Action: Root-caused: _close_panel is synchronous and blocks the loop, so 18 variants each
        trigger a sequential ~150-name yfinance download; yfinance rate-limits partway and
        hands later variants (PEAD, lead_lag) empty panels. Added a process-level panel
        cache in momentum_xs_v2._close_panel (one fetch per universe for the whole run).
Result: pytest 94/94. Relaunching clean v3 as the authoritative run.
        Valid v2 numbers carried forward: skew_25d hold=21 wf DSR 0.752 (train 0.006 -
        regime-dependent); momentum 6-1 (126d) train 0.354 / wf 0.307 (most consistent window).
Next: v3 -> write MASTER_REPORT with curated SME verdicts + recommendations.

## [19:05 PT] Decision (FINDING): FMP free tier daily quota exhausted -> PEAD wf deferred
Why: PEAD returned 0 trades even isolated + throttled. 0 trades = bug; investigated.
Action: Direct FMP probe.
Result: stable/earnings now returns HTTP 429 "Limit Reach. Please upgrade your plan" for
        ALL symbols. FMP free tier = 250 req/day, blown by v1/v2/v3 + standalone PEAD runs
        (each ~107 symbols). The clean PEAD train DSR 0.527 from v1 stands; wf is deferred
        to tomorrow's quota reset. Added 429 backoff-retry + lowered FMP concurrency to 3
        (right fix for bursts, but today's daily quota was already gone).
Next: tomorrow — add a DISK cache to PEAD earnings (one pull reused forever), run PEAD
      train+wf once on fresh quota. lead_lag came back clean: NO EDGE (DSR 0.025/0.008).

## [19:15 PT] Decision: Wrote curated MASTER_REPORT from clean isolated numbers
Why: The all-concurrent run rate-limits free APIs (VRP n=28, PEAD/lead_lag 0 in v3) — those
     are artifacts, not verdicts. Trust the isolated runs.
Action: Wrote data/backtest_reports/MASTER_REPORT.md with SME verdicts + recommendations.
Result: PROMOTE-eligible: vrp_harvest (paper, regime-gated, small sleeve). PROMISING: PEAD
        (train 0.527, wf pending quota). SANDBOX: momentum, skew (regime-dependent). NO
        EDGE: lead_lag, momentum 3-1. BLOCKED: insider (tier), squeeze (data). IC DEAD.
        Did NOT touch signal_registry. pytest 94/94.
Next: push follow-up commit to pc branch. Tomorrow: PEAD disk-cache + wf; VIX-term-structure
      regime gate for VRP; migrate live FMP modules to stable API.
